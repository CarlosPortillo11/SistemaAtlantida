@model SistemaAtlantida.Models.CuentaModel;
@{
    ViewData["Title"] = "Dashboard";
}

<div>
    <p>@Model.NombreTitular</p>
    @Html.ActionLink("Ver compras", "Index", "Compras", new {numeroTarjeta = Model.NumeroTarjeta});
    <p>Monto Reciente Total: @ViewBag.MontoRecienteTotal</p>
</div>
